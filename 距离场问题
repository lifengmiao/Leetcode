Walls and Gates
01 Matrix
Shortest Distance from All Buildings

从距离为0（起点）的所有点出发，BFS遍历。先把距离为0的所有点加入queue，如果某点的距离场值大于将要付的值，加入queue，否则跳过。
如果每次BFS遍历只有一个一个起点（Shortest Distance from All Buildings），不用比较距离场值与将要付的值，只要用visit记录访问过的点，只要该点
访问过，现在要付的值一定比已有的值大，所以直接跳过。
